// variables.js

const iam_url = process.env.IAM_URI;
const client_id = process.env.CLIENT_ID;
const client_psw = process.env.CLIENT_PSW;
const bootstrap_external = process.env.BOOTSTRAP_EXTERNAL;
// ... more variables

module.exports = {
    iam_url,
    client_id,
    client_psw,
    bootstrap_external
  // ... more variables
};
